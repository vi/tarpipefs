#include <stdio.h>
#include <string.h>

#include "tar.h"

/*int main() {

    char* str = "Hello, world\n";
    char* path = "hello.txt";


    write_tar_entry(path, strlen(path), 0100644, str, strlen(str));

    write_trailer();
    return 0;
} */
